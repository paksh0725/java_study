spring:
  datasource:
    url: jdbc:h2:mem:local;MODE=MySQL;
    username: sa
    password:
    driver-class-name: org.h2.Driver
logging:
  level:
    org:
      springframework:
        transaction:
          interceptor: trace